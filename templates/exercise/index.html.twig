{% extends 'base.html.twig' %}

{% block title %}Hello ExcerciseController!{% endblock %}

{% block body %}
    <h1>{{ theme.name }}</h1>

    <div class="form-wrapper">
        {{ form_start(form, {'action': path('app_exercise_test', {theme: theme.name})}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
